/* This is a dummy login page. NOT PRODUCTION SAFE~ */

<h2>Sign Up:</h2>
{{! This is super WIPish messing around currently }}
<form method="post">
	{{#if errors}}
	<ul>
		{{#each errors}}
		<li>{{this.msg}}</li>
		{{/each}}
	</ul>
	{{/if}}
	<input type="hidden" name="_csrf" value="{{csrfToken}}">
	<label for="username">Username:</label><input type="text" name="username" value="{{data.username}}"/>
	<label for="password">Password:</label><input type="password" name="password" value=""/>
	<label for="password">Confirm Password:</label><input type="password" name="passwordconfirm" value=""/>
	<input type="submit" value="Sign up"/>
</form>